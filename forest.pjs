import("Tween.lib");

float ground_x;
float timer;
float delta;

class Tree{
  public float x,y;
	Tree(float theX,float theY){
		x = theX;
		y = theY;
	}
}

void setup(){
  ground_x = 55.0;
	frameRate(30);
	size(1500,800);
	background(255);
	timer = millis();
}

void draw(){
	delta = millis() - timer;
	timer = millis();
	background(255);
}
